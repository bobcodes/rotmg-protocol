package lusyhadyz
{
      public function allOf(... rest) : Waha {
         var _loc2_:Array = rest;
         if(rest.length==1&&rest[0] is Array)
         {
            _loc2_=rest[0];
         }
         return new Wybiqidov(_loc2_);
      }
}