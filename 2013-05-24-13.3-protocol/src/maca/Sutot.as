package maca
{
   import komawowag.Silikyta;
   import kyco.Nun;
   import lagizizoq.Mys;
   import bidav.Duwuweb;
   import zelinoryv.Vib;
   import nyt.Myqujaf;
   import nyt.Fykoheja;
   import nyt.Weryliz;
   import nyt.Giz;
   import nyt.Dos;
   import nyt.Semumaci;
   import nyt.Fotuk;
   import nyt.Sefug;
   import com.company.assembleegameclient.game.GameSprite;
   import pyw.Metilira;
   import pyw.ZombifyDialog;
   import pyw.Cis;


   public class Sutot extends Object implements Silikyta
   {
      public function Sutot() {
         var _loc1_:* = false;
         var _loc2_:* = true;
         super();
         return;
      }

      public var cemohoqal:Nun;

      public var davyl:Mys;

      public var mediatorMap:Duwuweb;

      public function configure() : void {
         var _loc1_:* = false;
         var _loc2_:* = true;
         this.cemohoqal.map(Vib).pybe();
         this.davyl.map(Myqujaf).bez(Fykoheja);
         this.davyl.map(Weryliz).bez(Giz);
         this.davyl.map(Dos).bez(Semumaci);
         this.davyl.map(Fotuk).bez(Sefug);
         this.mediatorMap.map(GameSprite).zusu(Metilira);
         this.mediatorMap.map(ZombifyDialog).zusu(Cis);
         return;
      }
   }

}