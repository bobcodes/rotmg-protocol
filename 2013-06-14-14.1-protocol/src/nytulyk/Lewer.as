package nytulyk
{
   import hunavefeg.Qoheqa;
   import flash.events.IEventDispatcher;
   import hunavefeg.Hyne;
   import donaqyzas.Rabymuse;
   import flash.events.EventDispatcher;


   public class Lewer extends Object implements Qoheqa
   {
      public function Lewer(param1:IEventDispatcher=null) {
         var _loc2_:* = false;
         var _loc3_:* = true;
         super();
         this._eventDispatcher=(param1)||(new EventDispatcher());
         return;
      }

      private const mohabahi:String = Rabymuse.create(Lewer);

      private var _eventDispatcher:IEventDispatcher;

      public function extend(param1:Hyne) : void {
         var _loc2_:* = false;
         var _loc3_:* = true;
         param1.difas.map(IEventDispatcher).pylo(this._eventDispatcher);
         return;
      }

      public function toString() : String {
         return this.mohabahi;
      }
   }

}