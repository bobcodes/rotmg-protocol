package hygo
{
      public function not(param1:Object) : Kajehe {
         if(param1 is Kajehe)
         {
            return new Pyqihewod(param1 as Kajehe);
         }
         return not(equalTo(param1));
      }
}