package fubo
{
[CLASS20]   import bikyvym.Tihoc;
   import pogefeqeh.Wahovy;
   import wufyba.Cobyjowek;
   import zajenevik.Tif;
   import bup.Viqukinu;
   import zozimuk.Zuresasy;
   import ruf.Hoqilo;
   import zozimuk.Tukejyp;
   import golac.PackageButton;
   import golac.Lymehe;
   import golac.PackageOfferDialog;
   import golac.Jejok;
   import golac.Juc;
   import golac.Barewapo;
   import dob.Beqoq;
   import dob.Qano;
   import ruf.Paceto;
   import ruf.Sikigygy;
   import ruf.Sejorage;
   import ruf.Lal;
   import ruf.Ficygac;
   import ruf.Fyjehohy;
   import ruf.Cojadu;
   import ruf.Zur;
   import ruf.Niteho;
   import ruf.Licid;
   import ruf.Befyvalas;


   public class Jizyzy extends Object implements Tihoc
   {
      public function Jizyzy() {
         var _loc1_:* = true;
         var _loc2_:* = false;
         super();
         return;
      }

      public var viqelo:Wahovy;

      public var mediatorMap:Cobyjowek;

      public var pyv:Tif;

      public var watew:Viqukinu;

      public function configure() : void {
         var _loc1_:* = false;
         var _loc2_:* = true;
         this.viqelo.map(Zuresasy).fajuna();
         this.viqelo.map(Hoqilo).fajuna();
         this.viqelo.map(Tukejyp);
         this.mediatorMap.map(PackageButton).sab(Lymehe);
         this.mediatorMap.map(PackageOfferDialog).sab(Jejok);
         this.mediatorMap.map(Juc).sab(Barewapo);
         this.mediatorMap.map(Beqoq).sab(Qano);
         this.pyv.map(Paceto).paradi(Sikigygy).konalyhe(Cifurybos,Sejorage);
         this.pyv.map(Lal).paradi(Ficygac);
         this.pyv.map(Fyjehohy).paradi(Cojadu);
         this.pyv.map(Zur).paradi(Niteho);
         this.pyv.map(Licid).paradi(Befyvalas);
         this.watew.niw(Tukejyp);
         return;
      }
   }
[/CLASS]
}