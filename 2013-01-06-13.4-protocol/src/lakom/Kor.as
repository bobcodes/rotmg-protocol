package lakom
{
[CLASS543]   import bikyvym.Tihoc;
   import pogefeqeh.Wahovy;
   import zun.Vij;
   import fyrida.Rym;
   import wufyba.Cobyjowek;
   import setuv.Tufyz;
   import hine.Hesefaho;
   import setuv.Mocor;
   import tuzopite.Lyzepemif;
   import tuzopite.Diwewoqem;
   import tuzopite.Jomytejod;
   import tusidywuq.Selamehu;
   import tuzopite.Zet;
   import aaa.ActionMapperAbstract;
   import mywyvuryw.Text;
   import tuzopite.Lez;
   import tuzopite.Zylut;
   import tuzopite.Niveq;
   import hine.Ficezelum;
   import hine.Noqotyraq;
   import hine.Myr;
   import hine.Qafiji;
   import hine.Chat;
   import hine.Byzu;
   import hine.Fypefugyz;
   import hine.Terip;


   public class Kor extends Object implements Tihoc
   {
      public function Kor() {
         var _loc1_:* = false;
         var _loc2_:* = true;
         super();
         return;
      }

      public var viqelo:Wahovy;

      public var vybaduca:Vij;

      public var pyv:Rym;

      public var mediatorMap:Cobyjowek;

      public function configure() : void {
         var _loc1_:* = true;
         var _loc2_:* = false;
         this.viqelo.map(Tufyz).fajuna();
         this.viqelo.map(Kor).fajuna();
         this.viqelo.map(Hesefaho).fajuna();
         this.viqelo.map(Mocor).fajuna();
         this.viqelo.map(Lyzepemif).fajuna();
         this.viqelo.map(Diwewoqem).fajuna();
         this.viqelo.map(Jomytejod).fajuna();
         this.pyv.map(Selamehu).paradi(Zet);
         this.vybaduca.map(ActionMapperAbstract.TEXT).kiho(Text).kimy(Lez);
         this.pyv.map(Zylut).paradi(Niveq);
         this.mediatorMap.map(Ficezelum).sab(Noqotyraq);
         this.mediatorMap.map(Myr).sab(Qafiji);
         this.mediatorMap.map(Chat).sab(Byzu);
         this.mediatorMap.map(Fypefugyz).sab(Terip);
         return;
      }
   }
[/CLASS]
}